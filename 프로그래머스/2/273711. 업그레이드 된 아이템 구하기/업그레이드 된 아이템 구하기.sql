SELECT 
    C.ITEM_ID,
    C.ITEM_NAME,
    C.RARITY
FROM 
    ITEM_TREE T
JOIN 
    ITEM_INFO P
ON
    T.PARENT_ITEM_ID = P.ITEM_ID
JOIN
    ITEM_INFO C
ON
    T.ITEM_ID = C.ITEM_ID
WHERE
    P.RARITY = 'RARE'
ORDER BY
    1 DESC;